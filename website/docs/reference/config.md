# Configuration

Task can be configured using a `.taskrc.yml` file or environment variables.

## Global Configuration

You can configure global settings in your home directory.

## Output

You can configure the output style.

```yaml
output: prefixed
```

## Silent Mode

You can silence the output of tasks.

```yaml
silent: true
```
